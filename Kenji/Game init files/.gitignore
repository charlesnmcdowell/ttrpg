# Local campaign save (copy from kenji_state.example.json)
kenji_state.json

# Generated LLM brief (optional — remove this line if you want AI_CONTEXT.md in version control)
AI_CONTEXT.md

# Per-character gamemode call counter / sampler state (regenerated each run)
_gamemode_meta_*.json

# Python
__pycache__/
*.pyc

# Arc pointer session logs (receipt output — regenerate with run_arc_pointer.py)
logs/*.txt
!logs/.gitkeep

# Secrets (never commit — use env or OS keychain)
key.txt

# IDE
.cursor/
